{
  "name": "snake-massi",
  "version": "1.0.0",
  "private": true,
  "description": "Jeu Snake mobile et installable, hébergé sur GitHub Pages.",
  "scripts": {
    "check": "node --check game.js && node --check sw.js"
  }
}
